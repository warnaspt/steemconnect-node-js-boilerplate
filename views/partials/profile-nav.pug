section.container.profile__subnav
  .row
    a.btn.btn-dark(href=`/@${username}`) Blog
    a.btn.btn-dark(href=`/@${username}/transfers`) Transferencias
    if user
      if user.name == username
        a.btn.btn-dark(href=`/@${username}/feed`) Meu Feed
